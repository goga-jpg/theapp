rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow anyone to submit the contact form, no read/edit access
    match /contacts/{document=**} {
      allow create: if true;
      allow read, update, delete: if false;
    }
  }
}
